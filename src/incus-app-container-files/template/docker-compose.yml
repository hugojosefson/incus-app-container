services:
  app:
    image: docker.io/traefik/whoami
    environment:
      - PORT=80
    ports: ["80:80"]
    labels:
      - io.containers.autoupdate=registry
#   volumes:
#     - /appdata/data:/data:ro    # left side is inside lxc, right is inside container
